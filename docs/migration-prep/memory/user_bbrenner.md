---
name: user profile — bbrenner
description: Who bbrenner is and how they prefer to collaborate
type: user
---

- Works on ML infrastructure / inference systems (specifically the stateful Mamba SGLang fork)
- Prefers brief, direct responses — dislikes filler and summaries of what was just done
- Uses `/branch` to start focused sub-sessions for isolated tasks (e.g., PR review)
- Comfortable delegating large tasks to parallel agents ("go for it" → launch agent teams)
- Asks confirmation questions before major milestones but moves fast once aligned
- Typos are common in messages ("my mistage", "gp for it") — interpret charitably
- Works on a GCloud GPU instance; may SSH in via Cloudflare tunnel
- Collaborates with at least one other user (jeanclawdai) who manages model files at `/home/jeanclawdai/`
- Uses GitHub (KHAEntertainment org) for the sglang-mamba project
- Interested in stateful LLM inference, snapshot-based session persistence, and branching/rollback

**How to apply:** Keep responses concise. Propose rather than ask when scope is clear. When bbrenner says "go for it" or similar, proceed without asking for more confirmation — they've approved.
