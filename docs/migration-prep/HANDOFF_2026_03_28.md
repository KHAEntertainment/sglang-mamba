# 🚀 SESSION HANDOFF: RunPod A100 Migration

**Date:** 2026-03-28
**Target Environment:** RunPod 4x A100 (or single A100/A16) — `sm86` or `sm80` Architecture
**Workspace Path:** `/root/sglang-mamba/`

## 1. What We Just Accomplished
* We hit a quota wall on GCP (`1.0` V100 max globally) and Vultr, so we migrated to **RunPod**!
* We successfully transferred all of the `~/.claude/projects/` memory files over SSH.
* We spun up a background `vultr_startup.sh` script to install `pip` dependencies, clone `sglang-mamba`, and attempt to pre-download the Granite model.

## 2. The Current Problem / Blocker
The `vultr_startup.sh` script likely **hung or failed** during the HuggingFace `snapshot_download` step because it did not have an authenticated `HF_TOKEN`.

## 3. Immediate Next Steps for the NEW Agent Session
If you are the brand new Claude / Antigravity agent reading this natively inside the RunPod container, follow these steps:

1. **Ask the user for their HuggingFace Token** (if they haven't provided it in the prompt).
2. **Execute HF Login:** Run `huggingface-cli login --token <TOKEN>` to authenticate.
3. **Finish the Download:** Run the python script to download the model into `/mnt/models/granite-4.0-h-tiny`:
   ```python
   from huggingface_hub import snapshot_download
   snapshot_download('ibm-granite/granite-4.0-tiny-mamba2-instruct', local_dir='/mnt/models/granite-4.0-h-tiny')
   ```
4. **Ensure Config is Right:** Double check that `test/phases/config.sh` has `MODEL_PATH=/mnt/models/granite-4.0-h-tiny`.
5. **START PHASE 1:** We are on an AMPERE (or newer) GPU now (`sm80`+), so the `sm70` Volta compatibility errors that blocked the V100 are completely gone! Open `test/phases/phase-01-stateless-inference-baseline.md` and immediately execute the stateless inference baseline test!

Good luck on the Ampere side! You've got this! 🏁
